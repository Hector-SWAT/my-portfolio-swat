<section id="sobre-mi" class="section-container">
  <div class="section-content">
    <h2 class="section-title">Sobre Mí</h2>
    <div class="about-grid">
      <div class="about-text">
        <p class="about-paragraph">
          Soy un desarrollador apasionado <span class="highlight-text">5 años de experiencia</span> 
          en programación y ciberseguridad, especializado en identificar, prevenir y mitigar vulnerabilidades para mantener entornos digitales seguros.
        </p>
        <p class="about-paragraph">
          Especializado en <span class="highlight-text">desarrollo web full-stack</span> y 
          <span class="highlight-text">auditorías de seguridad</span>, creando aplicaciones rápidas, escalables y blindadas contra amenazas.
        </p>
        <p class="about-paragraph">
          Mi objetivo es diseñar soluciones digitales eficientes, 
          innovadoras y seguras, cuidando cada detalle del código, la infraestructura y la experiencia del usuario.
        </p>
        <div class="stats-grid">
          <div class="stat-item">
            <span class="stat-number">20+</span>
            <span class="stat-label">Proyectos</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">5+</span>
            <span class="stat-label">Años Exp.</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">100%</span>
            <span class="stat-label">Dedicación</span>
          </div>
        </div>
      </div>

      <div class="about-image">
        <div class="card-container">
          <div class="card">
            <div class="card-front">
              <img 
                src=/images/hector-perfil.png 
                alt="Héctor Hernández" 
                class="profile-image"
                width="350"
                height="350"
              />
            </div>
            <div class="card-back">
              <img 
                src=/images/yugi-oh-back.jpg
                alt="Carta Yu-Gi-Oh!" 
                class="yugioh-image"
                width="350"
                height="350"
              />
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Estilos base */
  .section-container {
    padding: 6rem 1rem;
    background: linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%);
  }

  .section-content {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-title {
    font-size: 2.5rem;
    background: linear-gradient(45deg, #FFD700, #DAA520);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    text-align: center;
    margin-bottom: 3rem;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .about-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  /* Estilos para el efecto de carta giratoria */
  .card-container {
    perspective: 1000px;
    width: 450px;
    height: 650px;
    margin: 0 auto;
  }

  .card {
    position: relative;
    width: 100%;
    height: 100%;
    transform-style: preserve-3d;
    transition: transform 1.5s ease;
    animation: rotate 10s infinite ease-in-out;
  }

  .card:hover {
    animation-play-state: paused;
  }

  .card-front, .card-back {
    position: absolute;
    width: 100%;
    height: 100%;
    backface-visibility: hidden;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(218, 165, 32, 0.3);
  }

  .card-back {
    transform: rotateY(180deg);
    background: #000;
  }

  .profile-image, .yugioh-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @keyframes rotate {
    0%, 100% { transform: rotateY(0deg); }
    50% { transform: rotateY(180deg); }
  }

  /* Estilos del texto */
  .about-paragraph {
    font-size: 1.1rem;
    line-height: 1.7;
    color: rgba(229, 229, 229, 0.9);
    margin-bottom: 1.5rem;
  }

  .highlight-text {
    color: #DAA520;
    font-weight: 600;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin-top: 2rem;
  }

  .stat-item {
    background: rgba(218, 165, 32, 0.1);
    border: 1px solid rgba(218, 165, 32, 0.2);
    border-radius: 10px;
    padding: 1.2rem;
    text-align: center;
    transition: transform 0.3s ease;
  }

  .stat-item:hover {
    transform: translateY(-5px);
  }

  .stat-number {
    font-size: 1.8rem;
    font-weight: 700;
    color: #FFD700;
    display: block;
  }

  .stat-label {
    font-size: 0.9rem;
    color: rgba(229, 229, 229, 0.8);
  }
  /* ABOUT SECTION */
.section-container {
  padding: var(--about-padding);
  background: linear-gradient(135deg, #0f0f0f 0%, #1a1a1a 100%);
  width: 100%;
  overflow: hidden;
}

.section-content {
  max-width: 1200px;
  margin: 0 auto;
  width: 100%;
}

.section-title {
  font-size: var(--about-title-size);
  background: linear-gradient(45deg, #FFD700, #DAA520);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  text-align: center;
  margin-bottom: 3rem;
  font-weight: 700;
}

.about-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 3rem;
  align-items: center;
  width: 100%;
}

/* ABOUT TEXT */
.about-text {
  width: 100%;
}

.about-paragraph {
  font-size: var(--about-text-size);
  line-height: 1.7;
  color: rgba(229, 229, 229, 0.9);
  margin-bottom: 1.5rem;
}

.highlight-text {
  color: #DAA520;
  font-weight: 600;
  position: relative;
}

.highlight-text::after {
  content: '';
  position: absolute;
  bottom: -2px;
  left: 0;
  width: 100%;
  height: 2px;
  background: linear-gradient(90deg, #DAA520, transparent);
  opacity: 0.7;
}

/* STATS GRID */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-top: 2rem;
}

.stat-item {
  background: rgba(218, 165, 32, 0.1);
  border: 1px solid rgba(218, 165, 32, 0.2);
  border-radius: 10px;
  padding: 1.2rem;
  text-align: center;
  transition: transform 0.3s ease;
  backdrop-filter: blur(10px);
}

.stat-item:hover {
  transform: translateY(-5px);
  background: rgba(218, 165, 32, 0.15);
}

.stat-number {
  font-size: var(--stat-font-size);
  font-weight: 700;
  color: #FFD700;
  display: block;
}

.stat-label {
  font-size: 0.9rem;
  color: rgba(229, 229, 229, 0.8);
  margin-top: 0.5rem;
}

/* ABOUT IMAGE - CARTA RESPONSIVE */
.about-image {
  display: flex;
  justify-content: center;
  width: 100%;
}

.card-container {
  perspective: 1000px;
  width: var(--card-width);
  height: var(--card-height);
  margin: 0 auto;
  max-width: 100%;
}

.card {
  position: relative;
  width: 100%;
  height: 100%;
  transform-style: preserve-3d;
  transition: transform 1.5s ease;
  animation: rotate 10s infinite ease-in-out;
}

.card:hover {
  animation-play-state: paused;
}

.card-front, 
.card-back {
  position: absolute;
  width: 100%;
  height: 100%;
  backface-visibility: hidden;
  border-radius: 20px;
  overflow: hidden;
  box-shadow: 0 10px 30px rgba(218, 165, 32, 0.3);
}

.card-back {
  transform: rotateY(180deg);
  background: #000;
}

.profile-image, 
.yugioh-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

@keyframes rotate {
  0%, 100% { 
    transform: rotateY(0deg); 
  }
  50% { 
    transform: rotateY(180deg); 
  }
}

/* MEDIA QUERIES RESPONSIVOS PARA ABOUT */

/* Desktop large */
@media (min-width: 1200px) {
  .about-grid {
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
  }
}

/* Desktop */
@media (min-width: 768px) {
  .about-grid {
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
  }
}

/* Tablets */
@media (max-width: 1024px) and (min-width: 769px) {
  :root {
    --card-width: 350px;
    --card-height: 500px;
    --about-padding: 5rem 1.5rem;
  }
}

/* Mobile Large (768px) */
@media (max-width: 768px) {
  :root {
    --about-padding: 4rem 1rem;
    --about-title-size: 2rem;
    --about-text-size: 1rem;
    --card-width: 280px;
    --card-height: 400px;
    --stat-font-size: 1.5rem;
  }
  
  .about-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
    text-align: center;
  }
  
  .section-container {
    padding: var(--about-padding);
  }
  
  .section-title {
    margin-bottom: 2rem;
  }
  
  .about-paragraph {
    text-align: left;
  }
  
  .stats-grid {
    gap: 0.8rem;
    margin-top: 1.5rem;
  }
  
  .stat-item {
    padding: 1rem 0.5rem;
  }
  
  .stat-label {
    font-size: 0.8rem;
  }
}

/* Mobile Medium (480px) */
@media (max-width: 480px) {
  :root {
    --about-padding: 3rem 0.75rem;
    --about-title-size: 1.8rem;
    --card-width: 240px;
    --card-height: 340px;
    --stat-font-size: 1.3rem;
  }
  
  .section-container {
    padding: var(--about-padding);
  }
  
  .about-paragraph {
    font-size: 0.95rem;
    margin-bottom: 1rem;
  }
  
  .stats-grid {
    gap: 0.6rem;
    margin-top: 1rem;
  }
  
  .stat-item {
    padding: 0.8rem 0.4rem;
    border-radius: 8px;
  }
  
  .stat-number {
    font-size: 1.3rem;
  }
  
  .stat-label {
    font-size: 0.75rem;
  }
  
  .card-container {
    margin: 0 auto;
  }
}

/* Mobile Small (360px) */
@media (max-width: 360px) {
  :root {
    --about-padding: 2.5rem 0.5rem;
    --about-title-size: 1.6rem;
    --card-width: 200px;
    --card-height: 280px;
    --stat-font-size: 1.2rem;
  }
  
  .section-container {
    padding: var(--about-padding);
  }
  
  .section-content {
    padding: 0 0.5rem;
  }
  
  .about-paragraph {
    font-size: 0.9rem;
    line-height: 1.6;
  }
  
  .stats-grid {
    grid-template-columns: 1fr;
    gap: 0.8rem;
    max-width: 200px;
    margin: 1rem auto 0;
  }
  
  .stat-item {
    padding: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    text-align: left;
  }
  
  .stat-number {
    font-size: 1.2rem;
  }
  
  .stat-label {
    font-size: 0.8rem;
    margin-top: 0;
  }
  
  .card-front,
  .card-back {
    border-radius: 15px;
  }
}

/* Optimización para pantallas muy anchas */
@media (min-width: 1400px) {
  .section-content {
    max-width: 1400px;
  }
  
  .about-grid {
    gap: 5rem;
  }
}

/* Landscape mobile */
@media (max-width: 768px) and (orientation: landscape) {
  :root {
    --about-padding: 2rem 1rem;
    --card-width: 200px;
    --card-height: 280px;
  }
  
  .about-grid {
    grid-template-columns: 1fr 0.8fr;
    gap: 2rem;
    align-items: center;
  }
  
  .section-title {
    font-size: 1.8rem;
    margin-bottom: 1.5rem;
  }
  
  .about-paragraph {
    font-size: 0.9rem;
    margin-bottom: 1rem;
  }
  
  .stats-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 0.5rem;
    margin-top: 1rem;
  }
  
  .stat-item {
    padding: 0.6rem 0.3rem;
  }
}

/* Mejoras de accesibilidad */
@media (prefers-reduced-motion: reduce) {
  .card {
    animation: none;
  }
  
  .card:hover {
    animation: none;
  }
  
  .stat-item {
    transition: none;
  }
  
  .stat-item:hover {
    transform: none;
  }
}

/* Alto contraste */
@media (prefers-contrast: high) {
  .section-title {
    background: #FFD700;
    -webkit-background-clip: unset;
    background-clip: unset;
    -webkit-text-fill-color: unset;
    color: #FFD700;
  }
  
  .highlight-text {
    color: #FFD700;
  }
  
  .highlight-text::after {
    background: #DAA520;
  }
  
  .stat-item {
    background: rgba(218, 165, 32, 0.2);
    border: 2px solid #DAA520;
  }
}

/* Optimización para touch devices */
@media (hover: none) and (pointer: coarse) {
  .card:hover {
    animation-play-state: running;
  }
  
  .stat-item:hover {
    transform: none;
  }
}

/* Asegurar que las imágenes no se salgan */
@media (max-width: 768px) {
  .about-image {
    width: 100%;
    display: flex;
    justify-content: center;
    padding: 0 1rem;
  }
  
  .card-container {
    width: 100%;
    max-width: var(--card-width);
    height: var(--card-height);
  }
  
  .profile-image,
  .yugioh-image {
    object-fit: cover;
    object-position: center;
  }
}

/* Stats responsive específicos */
@media (max-width: 480px) {
  .stats-grid {
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    max-width: 250px;
    margin: 1.5rem auto 0;
  }
  
  .stat-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    text-align: left;
    padding: 1rem 1.2rem;
  }
  
  .stat-number,
  .stat-label {
    display: inline;
  }
}

/* Evitar overflow horizontal */
@media (max-width: 768px) {
  .section-container {
    overflow-x: hidden;
  }
  
  .about-grid {
    width: 100%;
    max-width: 100%;
  }
  
  .card-container {
    max-width: 100vw;
    width: 100%;
  }
}
</style>